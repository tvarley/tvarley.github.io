---
problemNumber: 100
title: Arranged probability
description: Find the smallest number of blue discs b such that the total number of discs n > 10^12 and b(b-1)/(n(n-1)) = 1/2.
difficulty: medium
date: 2024-04-12
technologies:
  - recurrence relations
  - pell equations
implementations:
  - language: cpp
    code: |
      #include <iostream>
      #include <cmath>

      long long arranged_probability() {
          // Use the recurrence for solutions to 2b(b-1) = n(n-1)
          // Find the smallest b where n > 10^12
          long long b = 15, n = 21;
          while (n <= 1000000000000LL) {
              long long b_new = 3 * b + 2 * n - 2;
              long long n_new = 4 * b + 3 * n - 3;
              b = b_new;
              n = n_new;
          }
          return b;
      }
    githubLink: https://github.com/tvarley/euler/blob/master/cpp/src/euler100.cpp
    performance: ~1ms
  - language: go
    code: |
      package main

      func arrangedProbability() int64 {
          b, n := int64(15), int64(21)
          for n <= 1000000000000 {
              b_new := 3*b + 2*n - 2
              n_new := 4*b + 3*n - 3
              b = b_new
              n = n_new
          }
          return b
      }
    githubLink: https://github.com/tvarley/euler/blob/master/go/src/euler100.go
    performance: ~1ms
  - language: java
    code: |
      public class Euler100 {
          static long arrangedProbability() {
              long b = 15, n = 21;
              while (n <= 1000000000000L) {
                  long b_new = 3 * b + 2 * n - 2;
                  long n_new = 4 * b + 3 * n - 3;
                  b = b_new;
                  n = n_new;
              }
              return b;
          }
      }
    githubLink: https://github.com/tvarley/euler/blob/master/java/src/euler100.java
    performance: ~1ms
  - language: javascript
    code: |
      function arranged_probability() {
        let b = 15n, n = 21n;
        const limit = 1000000000000n;
        while (n <= limit) {
          const b_new = 3n * b + 2n * n - 2n;
          const n_new = 4n * b + 3n * n - 3n;
          b = b_new;
          n = n_new;
        }
        return Number(b);
      }
    githubLink: https://github.com/tvarley/euler/blob/master/javascript/src/euler/solution100.js
    performance: ~1ms
  - language: python
    code: |
      def solve():
          b, n = 15, 21
          while n <= 10**12:
              b_new = 3 * b + 2 * n - 2
              n_new = 4 * b + 3 * n - 3
              b, n = b_new, n_new
          return b
    githubLink: https://github.com/tvarley/euler/blob/master/python/src/euler100.py
    performance: ~1ms
  - language: rust
    code: |
      pub fn arranged_probability() -> i64 {
          let mut b: i64 = 15;
          let mut n: i64 = 21;
          while n <= 1_000_000_000_000 {
              let b_new = 3 * b + 2 * n - 2;
              let n_new = 4 * b + 3 * n - 3;
              b = b_new;
              n = n_new;
          }
          b
      }
    githubLink: https://github.com/tvarley/euler/blob/master/rust/src/euler100.rs
    performance: ~1ms
tags:
  - pell equations
  - recurrence
  - probability
featured: false
showcase: false
---

## Solution Notes

### Mathematical Background

The problem requires finding integer solutions to b(b-1)/(n(n-1)) = 1/2, which simplifies to 2b(b-1) = n(n-1). This Pell-like equation has solutions generated by a recurrence relation.

### Algorithm Analysis

Start from known small solution (b=15, n=21) and iterate the recurrence b_new = 3b + 2n - 2, n_new = 4b + 3n - 3 until n > 10^12.

### Performance

Extremely fast (~1ms) due to simple arithmetic operations and few iterations.

### Key Insights

- Pell equation solutions follow linear recurrence
- Direct computation without solving quadratic each time

### Educational Value

Demonstrates Pell equations, recurrence relations, and efficient generation of integer solutions to Diophantine equations.